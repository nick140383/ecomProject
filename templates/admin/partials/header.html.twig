<nav class="navbar navbar-expand-lg navbar-light bg-white">
    <a class="navbar-brand" href="#">Accueil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>



    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href=" {{ path('admin_modele_chaussure') }}">chaussures</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_commentaires') }}">
                    <i class="fas fa-comments"></i>
                    commentaires</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_client') }}">
                    <i class="fas fa-users"></i>
                    clients
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_promotion_list') }}">
                    <i class=""></i>
                    promotions</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_ville_list') }}">
                    <i class=""></i>
                    villes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_commandes_retournes') }}">
                    <i class=""></i>
                    Commandes Retournee</a>
            </li>
            {# <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_retour') }}">
                    <i class=""></i>
                    Retours</a>
            </li> #}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_marque_list') }}">
                    <i class=""></i>
                    Marques</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_stocks') }}">
                    <i class=""></i>
                    Stock</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_commandes') }}">
                    <i class=""></i>
                    Commandes</a>
            </li>

        </ul>
        <ul class="navbar-nav mr-auto">
            <li class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('home') }}" target="_blank" class="nav-link">
                    <i class="fas fa-eye"></i>
                    view the website


                </a>
            </li>

            {% if app.user %}
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"
                    id="accountDropdownLink">{{ app.user.nom }}</a>

                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDopdownLink">
                    <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier mon profil</a>
                    <a href="{{ path('account_password') }}" class="dropdown-item">Modifier mon mot de passe</a>
                    <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                </div>
            </li>


            {% endif %}
        </ul>
    </div>


</nav>

{% for label,messages in app.flashes %}
<div class="container">
    <div class="alert alert-{{ label }} mt-4">
        {% for message in messages %}
        {{ message|raw }}
        {% endfor %}
        <button class="close" data-dissmis="alert">
            <span>&times;</span>
        </button>
    </div>
</div>
{% endfor %}