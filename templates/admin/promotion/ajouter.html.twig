{% extends 'admin/base.html.twig' %}

{% block title %}creer promotion!{% endblock %}

{% block body %}
    <div class="container">
        <h1>Créer promotion</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Créer  Promotion</button>
        {{ form_end(form) }}

    </div>

{% endblock %}
